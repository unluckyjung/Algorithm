#include<bits/stdc++.h>
using namespace std;

int main(){

    queue<int> q;
    q.push(4);
    q.pop();
    
    q.front();
    q.back();

    q.empty();  //비어있다면, return 1;
    q.size();
    return 0;
}