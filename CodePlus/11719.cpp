#include <bits/stdc++.h>

int main()
{
    char c;
    while ((c = getchar()) && c != EOF)
    {
        printf("%c", c);
    }

    // while(scanf("%c", &c) != EOF){
    //     printf("%c", c);
    // }
    return 0;
}